[project]
name = "mock-agentic-coding-exercise"
version = "0.1.0"
description = "A small mock agentic coding interview exercise for Python tool-calling loops."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
python_files = ["test_*.py"]
